export enum Role {
  OWNER = 'owner',
  ADMIN = 'admin',
  MODER = 'moder',
  BANKER = 'banker',
  STREAMER = 'streamer',
}

export const rolesToColors = ['yellow', 'red', 'blue', 'green', 'violet'];
