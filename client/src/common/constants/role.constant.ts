export enum Role {
  ADMIN = 'admin',
  MODER = 'moder',
  BANKER = 'banker',
  CONSUL = 'consul',
  STREAMER = 'streamer',
}

export const rolesToColors = ['red', 'blue', 'green', 'yellow', 'violet'];
