export enum Role {
  ADMIN = 'admin',
  MODER = 'moder',
  SPAWN = 'spawn',
  HUB = 'hub',
  END = 'end',
  STREAMER = 'streamer',
}

export const rolesToColors = ['red', 'blue', 'teal', 'pink', 'grape', 'violet'];
